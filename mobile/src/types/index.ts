export interface User{id:string;institution_id:string;institution_code:string;email:string;first_name:string;last_name:string;roles:string[]}
export interface LoginInput{institution_code:string;email:string;password:string}export interface LoginResponse{access_token:string;token_type:'bearer';expires_in:number}
export type RecordValue=Record<string,unknown>;export interface GuardianChild extends RecordValue{student_id:string;student_name:string;matriculation_number:string;programme_name:string|null;current_level:string|null;can_view_results:boolean;can_view_attendance:boolean;can_view_academic_performance:boolean;can_view_transcript:boolean;can_view_clearance:boolean}
export interface AdminDashboard extends RecordValue{institution_id:string;institution_name:string;total_students:number;active_students:number;graduated_students:number;total_lecturers:number;active_lecturers:number;total_programmes:number;total_courses:number;current_academic_session:string|null;current_semester:string|null;active_course_offerings:number;active_course_registrations:number;published_results:number;pending_result_approvals:number;confirmed_graduations:number;issued_transcripts:number;issued_certificates:number;pending_mandatory_clearances:number}
export interface AdminStudent{ id:string;matriculation_number:string;first_name:string;last_name:string;programme_id:string;programme_name:string;current_level:string|null;enrollment_status:string }
export interface AdminOffering{id:string;course_code:string;course_title:string;academic_session:string;semester:string;status:string}
export interface AdminStudentSummary extends RecordValue{student_id:string;identity:RecordValue;programme:RecordValue|null;current_level:string|null;enrollment_status:string;academic_performance:unknown;progression:unknown;graduation_eligibility:unknown;clearance:unknown}
export interface AdminOfferingSummary extends RecordValue{course_offering_id:string;course:RecordValue;academic_session:RecordValue;semester:RecordValue;lecturer_assignments:RecordValue[];registered_student_count:number;class_session_count:number;attendance_headline:RecordValue;assessment_component_count:number;examination_count:number;result_status_summary:Record<string,number>}
