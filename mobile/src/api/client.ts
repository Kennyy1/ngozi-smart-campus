import {authStorage} from '../storage/authStorage';
const base=(process.env.EXPO_PUBLIC_API_BASE_URL??'http://10.0.2.2:8000/api/v1').replace(/\/$/,'');let unauthorized:(()=>void)|undefined;
export class ApiError extends Error{constructor(message:string,public status:number){super(message);this.name='ApiError'}}
export const setUnauthorizedHandler=(handler:(()=>void)|undefined)=>{unauthorized=handler};
export async function request<T>(path:string,options:RequestInit={}):Promise<T>{const token=await authStorage.getToken();const headers=new Headers(options.headers);headers.set('Accept','application/json');if(options.body)headers.set('Content-Type','application/json');if(token)headers.set('Authorization',`Bearer ${token}`);let response:Response;try{response=await fetch(`${base}${path}`,{...options,headers})}catch{throw new ApiError('Unable to reach the campus service. Check your connection and try again.',0)}if(response.status===401){await authStorage.clear();unauthorized?.()}if(!response.ok){let body:unknown;try{body=await response.json()}catch{body=null}const detail=body&&typeof body==='object'&&'detail' in body&&typeof body.detail==='string'?body.detail:undefined;const message=response.status===403||response.status===404?'This information is unavailable or you do not have access.':detail??'The request could not be completed.';throw new ApiError(message,response.status)}return response.status===204?undefined as T:response.json() as Promise<T>}
