import {Link} from 'expo-router';import {Text,View} from 'react-native';import type {GuardianChild} from '../types';import {InfoCard} from './UI';export function ChildNav({child}:{child:GuardianChild}){const links=[['','Overview',true],['results','Results',child.can_view_results],['attendance','Attendance',child.can_view_attendance],['performance','Performance',child.can_view_academic_performance],['transcript','Transcript',child.can_view_transcript],['clearance','Clearance',child.can_view_clearance]] as const;return <View style={{gap:8}}>{links.filter(x=>x[2]).map(([path,label])=><Link key={path} href={`/guardian/child/${child.student_id}${path?`/${path}`:''}` as never} asChild><InfoCard><Text>{label}</Text></InfoCard></Link>)}</View>}
