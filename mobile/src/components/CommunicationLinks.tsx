import {useRouter} from 'expo-router';import {Text} from 'react-native';import {ActionCard,SectionHeader} from './UI';
export function CommunicationLinks({role,timetable=false,announcements=false,library=false}:{role:'student'|'lecturer'|'guardian'|'admin';timetable?:boolean;announcements?:boolean;library?:boolean}){const router=useRouter();const links=[...(library?[['library','Library']]:[]),...(announcements?[['announcements','Announcements']]:[]),...(timetable?[['timetable','Timetable']]:[]),['notifications','Notifications']];return <><SectionHeader>Campus services</SectionHeader>{links.map(([path,label])=><ActionCard key={path} label={`Open ${label}`} onPress={()=>router.push(`/${role}/${path}` as never)}><Text style={{fontWeight:'800'}}>{label}</Text><Text>View {label.toLowerCase()} →</Text></ActionCard>)}</>}
