import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('student lecturer and admin expose Library through More navigation',()=>{assert.match(read('app/student/more.tsx'),/\['library','Library'\]/);assert.match(read('app/lecturer/more.tsx'),/library\/>/);assert.match(read('app/admin/more.tsx'),/library\/>/)});
test('library uses actionable native cards, deterministic Back controls, search and own loans without rendering IDs',()=>{const source=read('src/components/LibraryScreen.tsx');assert.match(source,/accessibilityLabel={`View \$\{String\(x\.title\)\}`}/);assert.match(source,/onPress=\{\(\)=>setSelected\(x\)\}/);assert.match(source,/View Resource/);assert.match(source,/setSelected\(null\)/);assert.match(source,/router\.back\(\)/);assert.match(source,/Search library/);assert.match(source,/My Loans/);assert.doesNotMatch(source,/<Text[^>]*>\{String\(x\.id\)\}/)});
test('digital downloads carry authentication and use safe cache filenames',()=>{assert.match(read('src/api/portals.ts'),/libraryDownloadRequest/);const source=read('src/components/LibraryScreen.tsx');assert.match(source,/downloadAsync/);assert.match(source,/request\.headers/);assert.match(source,/replace\(\/\[\^a-zA-Z0-9\._-\]/)});
test('Expo SDK 54 remains configured',()=>{const pkg=JSON.parse(read('package.json'));assert.match(pkg.dependencies.expo,/54/) });
