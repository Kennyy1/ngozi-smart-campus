import {useState} from 'react';import {KeyboardAvoidingView,Platform,StyleSheet,Text,TextInput,View} from 'react-native';import {useRouter} from 'expo-router';import {ErrorState,PrimaryButton,Screen} from '../src/components/UI';import {routeFor,useAuth} from '../src/services/AuthContext';import {colors,radius,spacing} from '../src/theme';
export default function Login(){const [institution_code,setCode]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const {login}=useAuth();const router=useRouter();async function submit(){setBusy(true);setError('');try{const user=await login({institution_code,email,password});router.replace(routeFor(user.roles) as never)}catch(e){setError(e instanceof Error?e.message:'Sign in failed')}finally{setBusy(false)}}return <Screen><KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={s.wrap}><Text style={s.brand}>NSC</Text><Text accessibilityRole="header" style={s.title}>Welcome back</Text><Text style={s.help}>Sign in with your institutional account.</Text><View style={s.form}>{[['Institution code',institution_code,setCode,false],['Email address',email,setEmail,false],['Password',password,setPassword,true]].map(([label,value,setter,secure])=><View key={String(label)}><Text style={s.label}>{String(label)}</Text><TextInput accessibilityLabel={String(label)} autoCapitalize="none" secureTextEntry={Boolean(secure)} value={String(value)} onChangeText={setter as (v:string)=>void} style={s.input}/></View>)}{error&&<ErrorState message={error}/>}<PrimaryButton title={busy?'Signing in…':'Sign in'} onPress={()=>void submit()} disabled={busy}/></View></KeyboardAvoidingView></Screen>}
const s=StyleSheet.create({wrap:{paddingTop:60,gap:spacing.sm},brand:{width:64,height:64,textAlign:'center',textAlignVertical:'center',borderRadius:32,backgroundColor:colors.green,color:'white',fontWeight:'900',fontSize:20},title:{fontSize:32,fontWeight:'900',color:colors.green},help:{color:colors.muted},form:{gap:spacing.md,marginTop:spacing.lg},label:{fontWeight:'700',color:colors.text,marginBottom:6},input:{minHeight:50,borderWidth:1,borderColor:colors.border,borderRadius:radius.md,backgroundColor:'white',paddingHorizontal:spacing.md,fontSize:16}});
