import {useLocalSearchParams} from 'expo-router';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ListScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <ListScreen title="Published results" before={<BackAction label="Children"/>} load={()=>guardianApi.results(id)} fields={['course_code','course_title','academic_session','semester','final_score','grade','grade_point','passed']}/>}
