import {useLocalSearchParams} from 'expo-router';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {DetailScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <DetailScreen title="Academic performance" before={<BackAction label="Children"/>} load={()=>guardianApi.performance(id)} highlights={['current_gpa','cgpa','cumulative_attempted_units','cumulative_earned_units','academic_standing']}/>}
