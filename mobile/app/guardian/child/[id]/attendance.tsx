import {useLocalSearchParams} from 'expo-router';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ListScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <ListScreen title="Attendance" before={<BackAction label="Children"/>} load={()=>guardianApi.attendance(id)} fields={['course_code','course_title','total_sessions','present_count','absent_count','late_count','attendance_percentage']}/>}
