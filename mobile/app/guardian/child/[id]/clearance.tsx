import {useLocalSearchParams} from 'expo-router';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {DetailScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <DetailScreen title="Clearance" before={<BackAction label="Children"/>} load={()=>guardianApi.clearance(id)} highlights={['is_fully_cleared','pending_count']}/>}
