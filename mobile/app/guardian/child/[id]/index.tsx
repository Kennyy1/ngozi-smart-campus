import {useLocalSearchParams,useRouter} from 'expo-router';import {Text} from 'react-native';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ActionCard,EmptyState,ErrorState,InfoCard,LoadingState,RecordCard,Screen,SectionHeader} from '../../../../src/components/UI';import {useApi} from '../../../../src/hooks/useApi';import type {RecordValue} from '../../../../src/types';const sections=[['Results','results','can_view_results'],['Attendance','attendance','can_view_attendance'],['Performance','performance','can_view_academic_performance'],['Transcript','transcript','can_view_transcript'],['Clearance','clearance','can_view_clearance']];export default function Page(){const {id}=useLocalSearchParams<{id:string}>(),router=useRouter();const q=useApi(()=>guardianApi.overview(id));const child=(q.data?.child??{}) as RecordValue,allowed=sections.filter(x=>child[x[2]]===true);return <Screen title="Child overview"><BackAction label="Children" href="/guardian/children"/>{q.loading?<LoadingState/>:q.error?<ErrorState message={q.error.message} onRetry={q.retry}/>:q.data?<><InfoCard><Text style={{fontSize:19,fontWeight:'800'}}>{String(child.student_name??'Student')}</Text><RecordCard record={child} fields={['matriculation_number','programme_name','current_level','enrollment_status']}/></InfoCard><RecordCard record={q.data} fields={['result_count','attendance_percentage','current_gpa','cgpa','academic_standing']}/><SectionHeader>Academic</SectionHeader>{allowed.map(([label,path])=><ActionCard key={path} label={`Open ${label}`} onPress={()=>router.push(`/guardian/child/${id}/${path}` as never)}><Text style={{fontWeight:'800'}}>{label}</Text><Text>Open {label.toLowerCase()} →</Text></ActionCard>)}{!allowed.length&&<EmptyState message="You do not have permission to view academic sections."/>}</>:null}</Screen>}
