import {useLocalSearchParams} from 'expo-router';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {DetailScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <DetailScreen title="Child overview" before={<BackAction label="Children"/>} load={()=>guardianApi.overview(id)} highlights={['result_count','attendance_percentage','current_gpa','cgpa','academic_standing']}/>}
