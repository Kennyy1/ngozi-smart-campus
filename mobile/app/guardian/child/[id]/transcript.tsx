import {useLocalSearchParams} from 'expo-router';import {guardianApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {DetailScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <DetailScreen title="Computed transcript" before={<BackAction label="Child Overview" href={`/guardian/child/${id}`}/>} load={()=>guardianApi.transcript(id)} highlights={['student_name','programme_name','cgpa','academic_standing']}/>}
