import {Link} from 'expo-router';import {Text} from 'react-native';import {guardianApi} from '../../src/api/portals';import {EmptyState,ErrorState,InfoCard,LoadingState,RecordCard,Screen} from '../../src/components/UI';import {useApi} from '../../src/hooks/useApi';export default function Page(){const q=useApi(guardianApi.children);return <Screen title="Linked children">{q.loading?<LoadingState/>:q.error?<ErrorState message={q.error.message} onRetry={q.retry}/>:q.data?.length?q.data.map(x=><Link key={x.student_id} href={`/guardian/child/${x.student_id}` as never} asChild><InfoCard><Text style={{fontWeight:'800'}}>{x.student_name}</Text><RecordCard record={x} fields={['matriculation_number','programme_name','current_level']}/></InfoCard></Link>):<EmptyState message="No verified child relationships are available."/>}</Screen>}
