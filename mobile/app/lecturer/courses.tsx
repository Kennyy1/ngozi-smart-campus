import {Link} from 'expo-router';import {Text} from 'react-native';import {lecturerApi} from '../../src/api/portals';import {EmptyState,ErrorState,InfoCard,LoadingState,RecordCard,Screen} from '../../src/components/UI';import {useApi} from '../../src/hooks/useApi';export default function Page(){const q=useApi(lecturerApi.courses);return <Screen title="Assigned courses">{q.loading?<LoadingState/>:q.error?<ErrorState message={q.error.message} onRetry={q.retry}/>:q.data?.length?q.data.map((x,i)=><Link key={String(x.course_offering_id??i)} href={`/lecturer/course/${x.course_offering_id}/students` as never} asChild><InfoCard><Text style={{fontWeight:'800'}}>{String(x.course_code)} · {String(x.course_title)}</Text><RecordCard record={x} fields={['academic_session','semester','registered_student_count','status']}/></InfoCard></Link>):<EmptyState/>}</Screen>}
