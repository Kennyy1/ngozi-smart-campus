import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ListScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <ListScreen title="Course attendance" before={<BackAction label="Courses"/>} load={()=>lecturerApi.attendance(id)} fields={['student_name','matriculation_number','total_sessions','present_count','absent_count','late_count','attendance_percentage']}/>}
