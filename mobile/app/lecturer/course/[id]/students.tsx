import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ListScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <ListScreen title="Course roster" before={<BackAction label="Courses"/>} load={()=>lecturerApi.students(id)} fields={['student_name','matriculation_number','current_level','registration_status']}/>}
