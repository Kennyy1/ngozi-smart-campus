import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../../src/api/portals';import {ScoreEntryScreen} from '../../../../../src/components/ScoreEntryScreen';export default function Page(){const {id,examinationId}=useLocalSearchParams<{id:string;examinationId:string}>();return <ScoreEntryScreen title="Examination score entry" backLabel="Examinations" backHref={`/lecturer/course/${id}/examinations`} requiredStatus="completed" load={()=>lecturerApi.examinationSheet(id,examinationId)} save={x=>lecturerApi.saveExamination(id,examinationId,x)}/>}
