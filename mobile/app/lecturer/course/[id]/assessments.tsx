import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ListScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <ListScreen title="Assessments" before={<BackAction label="Courses"/>} load={()=>lecturerApi.assessments(id)} fields={['title','type','maximum_score','weight','status','scored_student_count','unscored_student_count']}/>}
