import {useLocalSearchParams,useRouter} from 'expo-router';import {Text} from 'react-native';import {BackAction} from '../../../../src/components/AdminUI';import {ActionCard,Screen} from '../../../../src/components/UI';const actions=[['Students','students'],['Attendance','attendance'],['Assessments','assessments'],['Examinations','examinations'],['Results','results'],['Materials','materials']];export default function Page(){const {id}=useLocalSearchParams<{id:string}>(),router=useRouter();return <Screen title="Course Hub"><BackAction label="Courses" href="/lecturer/courses"/>{actions.map(([label,path])=><ActionCard key={path} label={`Open ${label}`} onPress={()=>router.push(`/lecturer/course/${id}/${path}` as never)}><Text style={{fontSize:18,fontWeight:'800'}}>{label}</Text><Text>Open {label.toLowerCase()} →</Text></ActionCard>)}</Screen>}
