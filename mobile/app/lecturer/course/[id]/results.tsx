import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {DetailScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <DetailScreen title="Published results" before={<BackAction label="Courses"/>} load={()=>lecturerApi.results(id)} highlights={['registered_student_count','published_result_count','missing_published_result_count']}/>}
