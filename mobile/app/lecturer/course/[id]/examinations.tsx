import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../src/api/portals';import {BackAction} from '../../../../src/components/AdminUI';import {ListScreen} from '../../../../src/components/ApiScreens';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();return <ListScreen title="Examinations" before={<BackAction label="Courses"/>} load={()=>lecturerApi.examinations(id)} fields={['title','type','maximum_score','weight','examination_date','status','scored_student_count','unscored_student_count']}/>}
