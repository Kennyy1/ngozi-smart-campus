import {useLocalSearchParams} from 'expo-router';import {lecturerApi} from '../../../../../src/api/portals';import {ScoreEntryScreen} from '../../../../../src/components/ScoreEntryScreen';export default function Page(){const {id,componentId}=useLocalSearchParams<{id:string;componentId:string}>();return <ScoreEntryScreen title="Assessment score entry" backLabel="Assessments" backHref={`/lecturer/course/${id}/assessments`} requiredStatus="published" load={()=>lecturerApi.assessmentSheet(id,componentId)} save={x=>lecturerApi.saveAssessment(id,componentId,x)}/>}
