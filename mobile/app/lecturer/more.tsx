import {lecturerApi} from '../../src/api/portals';import {ErrorState,LoadingState,Screen} from '../../src/components/UI';import {MoreScreen} from '../../src/components/MoreScreen';import {CommunicationLinks} from '../../src/components/CommunicationLinks';import {useApi} from '../../src/hooks/useApi';export default function LecturerAccount(){const q=useApi(lecturerApi.dashboard);if(q.loading)return <Screen><LoadingState/></Screen>;if(q.error)return <Screen><ErrorState message={q.error.message} onRetry={q.retry}/></Screen>;return <MoreScreen staffNumber={String(q.data?.staff_number??'')}><CommunicationLinks role="lecturer" timetable/></MoreScreen>}
