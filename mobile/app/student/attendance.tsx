import {studentApi} from '../../src/api/portals';import {ListScreen} from '../../src/components/ApiScreens';export default function Page(){return <ListScreen title="Attendance" load={studentApi.attendance} fields={['course_code','course_title','total_sessions','present_count','absent_count','late_count','attendance_percentage']}/>}
