import {studentApi} from '../../src/api/portals';import {BackAction} from '../../src/components/AdminUI';import {ListScreen} from '../../src/components/ApiScreens';export default function Page(){return <ListScreen title="Attendance" before={<BackAction label="More" href="/student/more"/>} load={studentApi.attendance} fields={['course_code','course_title','total_sessions','present_count','absent_count','late_count','attendance_percentage']}/>}
