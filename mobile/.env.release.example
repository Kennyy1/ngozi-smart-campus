# Public API endpoint embedded in the release application.
EXPO_PUBLIC_API_BASE_URL=https://campus.example.edu/api/v1
EXPO_PUBLIC_BUILD_PROFILE=production

# Signing credentials are intentionally absent. The documented EAS build uses
# encrypted EAS-managed Android credentials associated with the Expo account.
# Never place keystore passwords, private keys, or production secrets here.
